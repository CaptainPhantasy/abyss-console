# Fixes — the refactor ledger

This repo is going through a best-practices refactor, driven by two audited lists:

- **A — 23 issues that affect a human using this page to write code.** Items 1–21 were verified against
  the running app on 2026-09-16; items 22–23 were verified on 2026-09-17.
- **B — 17 coding weaknesses** — places where the page cannot do what a coding harness must.

Each row states the problem, the fix, and the proof that will flip its status from `open` to
`fixed`. A fix is only marked `fixed` when a command's output says so; the command is named in the
row. The work lands in waves — security first, then the change/diff path, then the experience.

## A. Human-use issues

| # | issue (verified) | fix | proof | status |
|---|---|---|---|---|
| 1 | **The helper ran any command a web page asked for.** CORS was `*` and `/run` checked only `confirm:true`; a page from another origin executed a command and a marker file appeared on disk. | Every route except `/` and `/health` now requires a per-install token (`x-abyss-token`), generated once into `~/.abyss-console/token` (0600; `ABYSS_TOKEN_FILE` overrides) and injected only into the page the helper serves; the exact helper origin and host are required, including preflight. Other local ports and opaque origins are refused; the served page cannot be framed or cached. A `file://` copy carries no token and cannot use helper routes. | `node --test tests/helper-auth.e2e.mjs` — 12/12 on 2026-09-17: missing/wrong tokens, foreign/opaque/other-local origins, DNS rebinding and cross-site embedding refused; authenticated reads and commands work in an isolated fixture. `node --test tests/helper.e2e.mjs` — 7/7 still green. | fixed |
| 2 | **The secret scrubber had a bypass.** It ran on the composed message only, so pinned text and tool results reached the model unscrubbed. | Text in every chat request is now scrubbed inside `callDeepSeek` — one choke point covering the system message, pinned text, every tool result, the history, text attachments, replayed tool arguments, reasoning and the draft; the fill-in-the-middle prompt gets the same treatment; both honour the Settings toggle. | `node tests/features.e2e.mjs` — 12/12, including "A2 the pinned secret is scrubbed before it leaves": a pin containing `password=hunter2hunter2` reached the stand-in as `password=REDACTED`, never raw. | fixed |
| 3 | **Two change mechanisms** (`### file:` whole-file blocks and unified diffs) with two review cards and no guidance on which the model should use. | The build contract now directs: diffs for changes to files that already exist; `### file: <path>` (or a bold filename line) only for files that do not exist yet. The write card warns when a rewrite would remove more lines than it adds, pointing at a diff instead. | `node tests/features.e2e.mjs` — "A23 the write card renders for the taught format" + "A4 the card shows a real diff and flags a big rewrite" (19/19 lane). | fixed |
| 4 | **The diff view was not a diff view.** It shows `+2 −0` and six changed lines by number — no context, no per-hunk view. | Compare now computes a real line diff (common ends trimmed, LCS on the middle, ±2 context lines, capped at 160 rows with the remainder counted) and renders − / + / context rows in colour with per-file counts; the patch path keeps its plain-language conflict report. | same lane — the A4 check reads the rendered diff (a removed line, the added line, the "removes 25 lines" nudge) after pressing compare on a real 25-line file. | fixed |
| 5 | **No git in the user interface** — no status, no diff against HEAD, no staging, no commit. | Git panel driven by helper routes: status, diff vs HEAD, stage, commit behind an explicit action; branch and change summary injected into the request context. | `node --test tests/git.e2e.mjs` (status/diff/commit against a scratch repo) | open |
| 6 | **Sessions cloned themselves on save** (new id every save; "cart session" existed seven times), and nothing showed which saved session the chat belonged to. | Saving an open session updates it in place (same id — proven on disk: the entry's messages went 3 → 6 while the library count stayed put), and a "current: <name>" chip always says which saved session the chat belongs to (or "unsaved chat"). | features lane — "A6 saving again updates in place, no clone". | fixed |
| 7 | **Opening a session replaced the chat** instead of opening a tab; the previous conversation left the view. | Sessions open as **tabs**: opening a session stashes the current chat into its own tab first, a tab strip lists every open chat with a × (the saved copy stays on disk), and switching tabs brings the other conversation back. | features lane — "A7 two sessions stay open as tabs" + "A7 switching tabs brings the other chat back" (the chip flips cart session → canvas tab two). | fixed |
| 8 | **The project map went stale silently.** Indexing is manual; no watching, no incremental update, no staleness warning. | The map carries its own age and the disk's: every file the walk returns reports its mtime, the tree reports the newest one, and coming back to the tab re-checks the folder. Files changed — the row turns amber and says "files changed — press re-index"; the project bar gains a **re-index** button that walks the folder again and clears the flag. | features lane — "A8/A9 changed files and edited pins are flagged" (touch a file, dispatch focus → amber flag + "indexed hh:mm" visible) and "A8 re-indexing clears the map's flag". | fixed |
| 9 | **Pins froze at the moment they were pinned.** Editing the pinned file keeps sending the old text, permanently, with no warning. | A pin remembers the path it was read from and when (the chip shows the age). Coming back to the tab compares every pinned file with the disk; a changed file turns the chip amber with "changed on disk" and offers a ↻ button that re-reads it in place. | features lane — "A8/A9 changed files and edited pins are flagged" (the pin flags after the file is rewritten) and "A9 re-reading a pin clears the flag". | fixed |
| 10 | **No `.gitignore` semantics.** The helper skipped a hardcoded list, not your `.gitignore`, so indexed contents did not match what you would commit. | The walk reads the project's own `.gitignore` at its root and leaves out what it names — plain names, directories, and `*` patterns (nested ignore files and `!` re-includes come later). | files lane — "the project's own .gitignore is respected": `skip.log` and `ignored-dir/x.txt` are absent from the tree, `keep.txt` is present. | fixed |
| 11 | **Four cost readouts** (meter, budget row, send estimate, per-turn line) — anxiety, not control. | One visible number — the "today $X of $Y" meter — plus the per-turn record line; the send estimate is now a **cost ▾** button (click for the token breakdown) and the strip's running Σ is gone. | features lane — "A11 one cost number, the rest behind a click": meter present, "cost ▾" present, the "send ≈" chip gone, the Σ gone. | fixed |
| 12 | **The forecast scared you on a cold session** — it priced input at the session's hit ratio, which is 0% until the cache warms, so turn one read ~10× the real bill ($0.000405 vs $0.000042 measured). | The forecast now blends the session's own hit ratio into the price, and a session with no history says exactly that: "0% cache hits (cold session — worst case; it improves as the cache warms)" — while a warm 90% session forecasts $0.0001 instead of $0.0004. | features lane — the F7 check asserts the cold label on a truly cold start; the warm path is visible whenever history exists. | fixed |
| 13 | **No keyboard model** — no command palette, no shortcuts for fork, pin, run, compare, trace, new session. | **⌘K / Ctrl-K** opens a command palette: type to filter, Enter runs the first match — new chat, open sessions, cost breakdown, sent inspector, change journal, run verify, apply & fix, both compare actions, trace, index, settings/chat, the daily idea. Escape closes it. | features lane — "A13 ⌘K opens a palette that runs actions": the key event opens it, "cost" + Enter ran the breakdown and the palette closed. | fixed |
| 14 | **No cancel, no progress for the slow things** — compare (two calls), run-and-fix, patch check, folder index and trace triage could hold the interface with one status line and no stop. | Every long operation now shows its state and can be stopped: the compare button becomes **cancel compare** while both calls run (aborting mid-flight leaves "cancelled" in both panels), the fix loop has its stop (B1), and the other slow paths label themselves ("walking…", "checking…", "asking the room…"). | features lane — "A14 the compare pair cancels on demand": cancel button mid-run, "cancelled" in the panel, the button back afterwards. | fixed |
| 15 | **Two overlapping "compare" actions** — "review with pro" and "compare flash vs pro" sat side by side doing nearly the same job with different mechanics and no guidance. | They now name their jobs: "compare flash vs pro" (send the same question to both models side by side, each with its own bill — for questions you have not asked yet) and "second opinion from pro" (hand the last exchange to pro and have it be blunt — for answers you already have). | features lane — "A15 the two compare actions explain themselves" (both titles + the new label). | fixed |
| 16 | **The 75-second room release was invisible** — nothing said the link was dropped and reconnected, so the first MCP call after a break looked like a stall. | The helper panel in Settings now says it: the room link drops after about 75 seconds of quiet and reconnects on the next call — that pause is the reconnect, not a hang. | features lane — "A16/A17 the helper's lifecycle is stated on the page". | fixed |
| 17 | **"Closes with the tab" is not what happens.** A supervised daemon outlives the tab and only releases the room bridge; the page does not say so. | The page states the truth, and a tab-close beacon now asks the helper to stop (`pagehide` → `POST /quit`, keepalive, token-gated — only a copy the helper served); the helper waits two seconds before stopping, so a reload is not a kill. launchd starts it again on the next visit, so nothing fights the supervisor. | features lane — the lifecycle check; the beacon fires only from a helper-served page (the test page has no token, so nothing is killed). | fixed |
| 18 | **The working set lives in the composer stack** — session, routing, project, verify, pins and tray all stack above the input; even collapsed, the compact row carries eight controls. | Working set moves to the rail / palette; the composer keeps the composer. | screenshot read back | open |
| 19 | **Deleting a session and unpinning were instant**, no confirm, no undo — while file writes got a full journal. | Both take two presses now: the first turns the × into "sure?" (and back again after four seconds), the second does it — nothing is destroyed on a stray click. | features lane — "A19 delete takes two presses and only then removes": 32 sessions on disk, still 32 after the first press, 31 after the second. | fixed |
| 20 | **No request inspector** — the forecast existed, but never what was actually sent, so "why did it answer that" was unanswerable without reading storage by hand. | A **sent ▾** button in the compact row lists the last five request bodies — model, message count, tool count, the full scrubbed JSON per request, and a copy button each. | features lane — "A20 the sent-request inspector shows the body": panel present, the body JSON visible, a copy button offered. | fixed |
| 21 | **Small ones:** tags are free text (no autocomplete — `#cart` vs `#carts`); session search needs a click, not Enter; export is Markdown-only and only for the current chat; the API key is collected through a `window.prompt`. | Tags now autocomplete from every tag you have ever used (a datalist on the tag box); the session search is live as you type — it never needed a click in this build; the `window.prompt` for the key is gone. Still open from this batch: export/import the whole library. | features lane — "A21 the tag box suggests tags you already use" (`["cart","money","fix"]` offered); the F6 check shows the search row appearing with no click. | open — 3 of 4 done; library export/import remains |
| 22 | *(2026-09-17)* **The cost footer was re-sent to the model as its own message.** The page appends "Cost of that turn — …" as an unflagged assistant row, and the recorder→request mapper filtered only `error` rows — so every later call carried harness text as the model's own words. The model then "confessed" to writing it and called real measurements fake. | The footer row is flagged display-only and the request mapper skips it — plus any legacy footer row, found by content. | `node tests/features.e2e.mjs` — "A22 the spend footer never rides into a request": every message in the outgoing request is checked. | fixed |
| 23 | *(2026-09-17)* **The write-card format did not match the build instruction.** The contract says "each preceded by a bold filename line"; the card detector looks for `### file:` — so a reply that followed the instruction got no card, and nothing could be written. | The contract now teaches `### file: <path>`, and the detector also accepts a bold filename line when it actually looks like one, so the old form keeps working. | same lane — "A23 the build contract teaches the write-card format" and "A23 the write card renders for the taught format" (a bold-filename reply produced the card). | fixed |

## B. Coding weaknesses

| # | weakness (verified) | fix | proof | status |
|---|---|---|---|---|
| 1 | **No autonomous apply → test → fix loop.** Applying, running, and feeding failures back are three hand-offs. | One button — **apply & fix** — applies what the last reply proposed (write blocks or a diff; every write journaled and undoable), runs the project's command, hands a failure back to the model, and repeats: up to 3 rounds, a per-round log in the chat, and a stop button that aborts the round in flight. | `node tests/features.e2e.mjs` — "B1 the fix loop applies, runs, feeds back and stops green" (round 1 runs and fails → the fix comes back → round 2 writes it → exit 0 → green) and "the loop really wrote the fix to disk" (21/21 lane). | fixed |
| 2 | **The model cannot search the project.** Search exists in the interface, not as a tool. | `search_project` tool: plain-text search returning `file:line` hits, capped at 60 shown / 200 found with the cap stated. | `node tests/features.e2e.mjs` — "B2 search_project returns file:line hits — hits for \"export const\": 2" (browser lane; the stand-in asks for the tool). | fixed |
| 3 | **No references tool** — renames are hopeful; the model guesses which callers exist. | `find_references` tool: text-level references with the matching lines. | same lane — "B3 find_references names where a symbol is used — references to \"total\": 1". | fixed |
| 4 | **Silent truncation** — 512 kB per file, 12k characters per tool result, 60 files per folder pull, none signalled. | Every capped read now signals: the 12k tool-result slice ends with "…[cut at 12,000 characters — ask for a smaller piece…]", a cut folder listing carries a note in the map, a walk stopped at its cap sets `truncated` on `/fs/tree`, per-file caps already reported their reason, and search states its own 60/200 caps. | features lane — "B4 a cut tool result says where it was cut" (a real 16 kB file came back with the marker at 12,000); files lane — "a cut listing says it was cut" (`/fs/tree?limit=5` → `truncated: true`). | fixed |
| 5 | **Whole-file writes were allowed** and could clobber a file's tail. | Diffs required above a size threshold (merges with A3). | `tests/files.e2e.mjs` | open |
| 6 | **The map is size-ranked with no retrieval** — context selection is guesswork. | Relevance ranking (recency, keyword hits, path heuristics) plus retrieval. | fixture test: the relevant file ranks above the big irrelevant one | open |
| 7 | **8 steps with no continuation protocol** — the model is not told it was cut off; nothing carries a task list forward. | The 8-step cap now says so in the transcript ("hit its 8-step cap — send 'continue' and it picks up…"), the leftover empty reply bubble is gone, and every tool result stays in context so the next message continues from them. | the lane's request capture shows the tool results riding into the next turn; the notice itself is display-only. | fixed |
| 8 | **No plan or task artifact across turns** — long work has no durable state beyond the transcript. | A small task store: goal, steps, done/not-done, visible on the page. | `tests/loop.e2e.mjs` | open |
| 9 | **No git awareness in context** — the model cannot see the branch or uncommitted changes. | Branch and uncommitted-change summary injected (merges with A5). | request-inspector evidence | open |
| 10 | **No concurrency** — one request at a time; multi-file jobs serialised for no reason. | Independent reads run in parallel. | timing evidence in `tests/loop.e2e.mjs` | open |
| 11 | **Cost per turn, not per task** — a daily cap exists, nothing says "this task may spend $0.05". | Per-task budget with a hard stop and a running total. | `tests/loop.e2e.mjs` — task stops at its budget | open |
| 12 | **The tool contract is thin** — nothing tells the model to prefer diffs or reports which files it touched. | The contract states diff-first (A3) and now requires a `touched: <path>, <path>` line at the end of any reply that changes files. | features lane — the contract check asserts `touched:` rides in the system message. | fixed |
| 13 | **It cannot scaffold or run a test it just wrote.** | The fix loop runs whatever command the verify box holds — so a test the model wrote can be run by the loop and its failure handed straight back (merged with B1). | same lane — the loop ran `node check.js` from the project folder. | fixed |
| 14 | **No three-way merge / fuzzy patch** — an exact-match patch half-conflicts and is refused after any edit. | Fuzzy hunk matching with a threshold and a plain conflict report; never force a hunk. | `node --test tests/diff.unit.mjs` | open |
| 15 | **One verify command only** — "lint and test" becomes two manual rounds. | The verify box takes one command per line; the fix loop runs them in order with a line each and stops at the first failure (the **run** button still hands the whole thing to bash, so `&&` chains keep working). | features lane — "B15 the loop runs the verify lines one at a time" (two lines seeded; both reported, the second only runs after the first passes). | fixed |
| 16 | **Large-file caps unexplained** (merges with B4). | Signalled caps — see B4's evidence. | same as B4. | fixed |
| 17 | **Multi-turn memory is a transcript, not a plan** (merges with B8). | Durable task state. | same as B8 | open |

## Publishing — what is deliberately not here

- **Secrets and keys:** the working tree was scanned for `sk-…`, `ghp_…`, `AIza…`, JWT-shaped strings
  and private-key blocks; the only matches are the scrubber's own patterns and fake fixtures in tests.
  The helper's onboarding token and the page's API key live outside this tree, on the machine, by design.
- **Local-only artifacts, kept out with `.gitignore`:** `shots/` (desktop screenshots),
  `assets/background-source.tiff` (the 21 MB master), and test build outputs
  (`dist/test-page.html`, `dist/debug.html`; the shipped page `dist/deepseek-api-console.html` is here).
- **Personal paths** in user-visible strings were replaced with `~`. What remains, and why: the
  helper's default room paths (`ROOM_BRIDGE`, `ROOM_TOKEN_FILE` override both) and two launchd label
  strings in `src/mcp-engine.js` and the untouched `baseline/` page are machine-specific integration
  details, not credentials.
- **Session data, recipes and the write journal** live under `~/.abyss-console/` and `~/.cache/` —
  never in this tree.

## Proof protocol

The suite that must stay green for any merge (run from the repo root):

    node build.mjs --check                          # every piece parses
    node --test tests/roi.unit.mjs tests/redact.unit.mjs
    node --test tests/helper.e2e.mjs tests/attach.e2e.mjs tests/files.e2e.mjs
    node tests/dc-gate.e2e.mjs                      # 66 assertions
    node tests/roi.e2e.mjs                          # 3 ladder runs (needs a stand-in: build --api-base)
    node tests/features.e2e.mjs                     # 11 checks (needs the test build)

## Pre-merge review, 2026-09-17

The review tightened A1 to the exact helper origin, made the client follow the served port, restricted token forwarding to that exact origin, and refused replacement of an unreadable or invalid existing token. A2 now also scrubs replayed tool arguments and reasoning, and the daily-idea path honors the scrub toggle. Image contents and unknown secret formats are outside the text scrubber.

Current checks: 27 unit tests, 12 isolated helper-security tests, all 66 permission-gate assertions, source parse checks, and an in-app browser check on an isolated helper and fake API. The browser indexed two fixture files, executed search_project and find_references, and showed the expected two search hits and one reference. The older proof counts above are retained author-reported history where not explicitly replaced by these current checks. No paid model call was made. The full older browser suite and live MCP room integration were not rerun in this review.

The final review also included preview commit 5e350735c9c3579c7429470ec24b244dd4b53254 (A22/A23). Current request-mapper tests prove both legacy and flagged footers are excluded, and file-proposal tests cover the documented heading, legacy bold filename, and ordinary bold prose.
